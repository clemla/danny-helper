services:
  bot:
    container_name: danny-helper

    build:
      context: .

    environment:
      NODE_ENV: production

    restart: unless-stopped

    volumes:
      - .:/app
      - /app/node_modules

    command: "bun run start"
